// import { defineConfig } from '../config/define-config';
// import type { RuleMeta, TailwindConfig, TailwindUserConfig } from '../types/config.types';

// export class TwinHandler {
//   private _rules: any[];
//   private _config: TailwindConfig;
//   mode: 'build' | 'runtime';

//   constructor(userConfig: TailwindUserConfig, mode: 'build' | 'runtime' = 'runtime') {
//     this._config = defineConfig(userConfig);
//     this.mode = mode;
//     this._rules = [];
//   }
// }

// interface TwinHandlerRule {
//   pattern: string;
//   config: RuleMeta;
//   resolver: 
// }