import { primitivesNativewindView } from './primitives-nativewind.view';

describe('primitivesNativewindView', () => {
  it('should work', () => {
    expect(primitivesNativewindView()).toEqual('primitives-nativewind.view');
  });
});
