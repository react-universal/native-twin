import { View as ReactNativeView } from 'react-native';
import { ForwardedStyledComponent } from '@universal-labs/styled';
declare const View: ForwardedStyledComponent<typeof ReactNativeView>;
export default View;
