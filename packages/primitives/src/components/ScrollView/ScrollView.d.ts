import { ScrollView as ReactNativeScrollView } from 'react-native';
import { ForwardedStyledComponent } from '@universal-labs/styled';
declare const ScrollView: ForwardedStyledComponent<ReactNativeScrollView>;
export default ScrollView;
