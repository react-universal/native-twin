import type { __Theme__ } from '../../types/theme.types';

export const resizeMode = {
  cover: 'cover',
  contain: 'contain',
  stretch: 'stretch',
  repeat: 'repeat',
  center: 'center',
} satisfies __Theme__['resizeMode'];
