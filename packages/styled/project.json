{
  "name": "styled",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/styled/src",
  "projectType": "library",
  "targets": {
    "build:libs": {
      "executor": "@nrwl/vite:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "packages/styled/build"
      }
    },
    "dev": {
      "executor": "@nrwl/vite:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "packages/styled/build",
        "watch": true,
        "emptyOutDir": false
      }
    },
    "test": {
      "executor": "@nrwl/vite:test",
      "outputs": ["coverage/packages/styled"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../coverage/packages/styled"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/styled/**/*.ts"]
      }
    }
  },
  "tags": []
}
