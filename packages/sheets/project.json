{
  "name": "stylesheets",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/sheets/src",
  "projectType": "library",
  "targets": {
    "build:libs": {
      "executor": "@nrwl/vite:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "packages/sheets/build"
      }
    },
    "dev": {
      "executor": "@nrwl/vite:build",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "packages/sheets/build",
        "watch": true
      }
    },
    "test": {
      "executor": "@nrwl/vite:test",
      "outputs": ["coverage/packages/sheets"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../coverage/packages/sheets"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/sheets/**/*.ts"]
      }
    }
  },
  "tags": []
}
