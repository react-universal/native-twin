import { tokenIdentity } from '../css/tokens';

const flexAlign = tokenIdentity('flex-align');
const dimension = tokenIdentity('dimension');
const cursor = tokenIdentity('cursor');
const shadowIOS = tokenIdentity('shadow-ios');
const transformItem = tokenIdentity('transformItem');
const styleAuto = tokenIdentity('style-auto');
const styleNone = tokenIdentity('style-none');
const flexWrap = tokenIdentity('flex-wrap');

export const declValue = {
  flexAlign,
  dimension,
  cursor,
  shadowIOS,
  transformItem,
  styleAuto,
  styleNone,
  flexWrap,
};
