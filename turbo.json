{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["build/**"],
      "cache": true
    },
    "dev": {
      "outputs": ["build/**"],
      "cache": false
    },
    "lint": {}
  }
}
