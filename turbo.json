{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["build/**"],
      "dependsOn": ["^build"],
      "cache": true
    },
    "dev": {
      "outputs": ["build/**"],
      "dependsOn": ["^dev"],
      "cache": false
    },
    "lint": {}
  }
}
